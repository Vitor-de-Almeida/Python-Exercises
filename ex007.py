#Create an algorithm that reads the two student grades and calculates their average.
n1 = float(input('Enter your first note: '))
n2 = float(input('Enter your second note: '))
av = (n1+n2)/2
print(f'The student average was {av:.1f}!')
